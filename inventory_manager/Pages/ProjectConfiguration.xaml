<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="inventory_manager.Pages.ProjectConfiguration"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:inventory_manager.Pages"
    xmlns:models="using:inventory_manager.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">

        <Grid>
            <TextBlock Text="Project Managment Configuration" FontSize="24" HorizontalAlignment="Center" Margin="10"/>

            <StackPanel Margin="20">
                
                <!-- Deliverable -->
                <StackPanel Orientation="Horizontal" Margin="0,10,0,5">
                    <TextBlock Text="Deliverable Items" FontSize="18" Margin="0,10,0,5"/>
                    <Button Content="Add Deliverable" Width="150" HorizontalAlignment="Left" Click="AddDeliverable_Click" Margin="10,0,0,0"/>
                </StackPanel>

                <Border BorderBrush="Black" BorderThickness="3" Margin="5,10,5,0" HorizontalAlignment="Left" Width="600">
                    <Grid Margin="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Name" Grid.Column="0" FontWeight="Bold" Margin="5,0,0,0"/>
                        <TextBlock Text="Description" Grid.Column="1" FontWeight="Bold" Margin="5,0,0,0"/>
                        <TextBlock Text="Project" Grid.Column="2" FontWeight="Bold" Margin="5,0,0,0"/>
                    </Grid>
                </Border>

                <ListView x:Name="DeliverablesListView" Height="200" Margin="5,0,0,10" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:DeliverableItem">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="200"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{x:Bind Name}" Grid.Column="0" TextWrapping="Wrap"/>
                                <TextBlock Text="{x:Bind Description}" Grid.Column="1" TextWrapping="Wrap"/>
                                <TextBlock Text="{x:Bind Project}" Grid.Column="2" TextWrapping="Wrap"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- People -->
                <StackPanel Orientation="Horizontal" Margin="0,10,0,5">
                    <TextBlock Text="People" FontSize="18"/>
                    <Button Content="Add Person" Width="150" HorizontalAlignment="Left" Click="AddPerson_Click" Margin="10,0,0,0"/>
                </StackPanel>

                <Border BorderBrush="Black" BorderThickness="3" Margin="5,10,5,0" HorizontalAlignment="Left" Width="600">
                    <Grid Margin="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Name" Grid.Column="0" FontWeight="Bold" Margin="5,0,0,0"/>
                        <TextBlock Text="Role" Grid.Column="1" FontWeight="Bold" Margin="5,0,0,0"/>
                        <TextBlock Text="Email" Grid.Column="2" FontWeight="Bold" Margin="5,0,0,0"/>
                    </Grid>
                </Border>

                <ListView x:Name="PeopleListView" Height="200" Margin="5,0,0,10" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:Person">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="200"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{x:Bind Name}" Grid.Column="0" TextWrapping="Wrap"/>
                                <TextBlock Text="{x:Bind Role}" Grid.Column="1" TextWrapping="Wrap"/>
                                <TextBlock Text="{x:Bind Email}" Grid.Column="2" TextWrapping="Wrap"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

            </StackPanel>
        </Grid>
    </ScrollViewer>

</Page>
