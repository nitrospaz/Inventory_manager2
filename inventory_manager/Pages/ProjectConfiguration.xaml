<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="inventory_manager.Pages.ProjectConfiguration"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:inventory_manager.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Form for Adding/Updating a Person -->
        <StackPanel Orientation="Vertical" Spacing="10">
            <TextBox x:Name="NameTextBox" PlaceholderText="Name" />
            <TextBox x:Name="EmailTextBox" PlaceholderText="Email" />
            <ComboBox x:Name="RoleComboBox" PlaceholderText="Role">
                <ComboBoxItem Content="SoftwareDeveloper" />
                <ComboBoxItem Content="Manager" />
                <ComboBoxItem Content="Designer" />
                <ComboBoxItem Content="Tester" />
                <ComboBoxItem Content="Other" />
            </ComboBox>
            <StackPanel Orientation="Horizontal" Spacing="10">
                <Button Content="Add" Click="AddPerson_Click" />
                <Button Content="Update" Click="UpdatePerson_Click" />
            </StackPanel>
        </StackPanel>

        <!-- List of Persons -->
        <ListView x:Name="PersonsListView" Grid.Row="1" SelectionChanged="PersonsListView_SelectionChanged">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Spacing="10">
                        <TextBlock Text="{Binding Name}" />
                        <TextBlock Text="{Binding Email}" />
                        <TextBlock Text="{Binding Role}" />
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <!-- Delete Button -->
        <Button Content="Delete" Grid.Row="2" HorizontalAlignment="Right" Click="DeletePerson_Click" />
    </Grid>
</Page>
