<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="inventory_manager.Pages.FinancePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:inventory_manager.Pages"
    xmlns:models="using:inventory_manager.Models"
    xmlns:converters="using:inventory_manager.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">

        <Grid>
            <TextBlock Text="Finance Management" FontSize="24" HorizontalAlignment="Center" Margin="10"/>

            <StackPanel Margin="20">
                <!-- Accounts -->
                <StackPanel Orientation="Horizontal" Margin="0,10,0,5">
                    <TextBlock Text="Accounts" FontSize="18"/>
                    <Button Content="Add Account" Width="150" HorizontalAlignment="Left" Click="AddAccount_Click" Margin="10,0,0,0"/>
                </StackPanel>

                <Border BorderBrush="Black" BorderThickness="3" Margin="5,10,5,0" HorizontalAlignment="Left" Width="600">
                    <Grid Margin="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Name" Grid.Column="0" FontWeight="Bold" Margin="5,0,0,0"/>
                        <TextBlock Text="Balance" Grid.Column="1" FontWeight="Bold" Margin="5,0,0,0"/>
                        <TextBlock Text="Last Transaction Date" Grid.Column="2" FontWeight="Bold" Margin="5,0,0,0"/>
                    </Grid>
                </Border>

                <ListView x:Name="AccountsListView" Height="200" Margin="5,0,0,10" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:FinanceAccount">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="200"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{x:Bind Name}" Grid.Column="0" TextWrapping="Wrap"/>
                                <TextBlock Text="{x:Bind Balance}" Grid.Column="1" TextWrapping="Wrap"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- Transactions -->
                <StackPanel Orientation="Horizontal" Margin="0,10,0,5">
                    <TextBlock Text="Transactions" FontSize="18" Margin="0,10,0,5"/>
                    <Button Content="Add Transaction" Width="150" HorizontalAlignment="Left" Click="AddTransaction_Click" Margin="10,0,0,0"/>
                </StackPanel>

                <Border BorderBrush="Black" BorderThickness="3" Margin="5,10,5,0" HorizontalAlignment="Left" Width="600">
                    <Grid Margin="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="200"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Description" Grid.Column="0" FontWeight="Bold" Margin="5,0,0,0"/>
                        <TextBlock Text="Amount" Grid.Column="1" FontWeight="Bold" Margin="5,0,0,0"/>
                        <TextBlock Text="Transaction Date" Grid.Column="2" FontWeight="Bold" Margin="5,0,0,0"/>
                    </Grid>
                </Border>

                <ListView x:Name="TransactionsListView" Height="200" Margin="5,0,0,10" ScrollViewer.VerticalScrollBarVisibility="Auto" ScrollViewer.HorizontalScrollBarVisibility="Auto">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="models:FinanceTransaction">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="200"/>
                                    <ColumnDefinition Width="200"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="{x:Bind Description}" Grid.Column="0" TextWrapping="Wrap"/>
                                <TextBlock Text="{x:Bind Amount}" Grid.Column="1" TextWrapping="Wrap"/>
                                <TextBlock Text="{x:Bind Date}" Grid.Column="2" TextWrapping="Wrap"/>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Grid>
    </ScrollViewer>

</Page>
